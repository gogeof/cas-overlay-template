package org.example.gogeof.random;

public interface RandomConstants {
    /**
     * 验证码存储常量，可以存储session等等
     */
    String STORE_CODE = "random_num";
    /**
     * 请求路径
     */
    String REQUEST_MAPPING = "/random";
}
